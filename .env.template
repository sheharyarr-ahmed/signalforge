# Copy to .env and fill in. Never commit .env (gitignored).
# See docs/DEPLOYMENT.md for where each value comes from.

# Hugging Face — Settings → Access Tokens (read / fine-grained inference token)
HF_API_TOKEN=

# Supabase — Project Settings → API
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Runtime
ENVIRONMENT=development
LOG_LEVEL=INFO
